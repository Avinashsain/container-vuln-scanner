global:
  scrape_interval: 15s        # how often Prometheus collects metrics

scrape_configs:
  - job_name: "pushgateway"
    honor_labels: true         # keep labels sent by our script
    static_configs:
      - targets: ["pushgateway:9091"]   # container name works as hostname