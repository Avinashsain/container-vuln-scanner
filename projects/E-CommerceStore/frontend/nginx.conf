events {}

http {
    include /etc/nginx/mime.types;

    server {
        listen 5005;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri /index.html;
        }
    }
}